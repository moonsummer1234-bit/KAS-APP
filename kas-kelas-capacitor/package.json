{
  "name": "kas-kelas-app",
  "version": "1.0.0",
  "description": "Aplikasi Kas Kelas & Pembayaran Buku - dibungkus dengan Capacitor agar bisa dipublikasikan ke Google Play Store",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/preferences": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
